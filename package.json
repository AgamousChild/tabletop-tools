{
  "name": "tabletop-tools",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter no-cheat-server --filter no-cheat-client --filter versus-server --filter versus-client --filter list-builder-server --filter list-builder-client --filter game-tracker-server --filter game-tracker-client --filter tournament-server --filter tournament-client dev",
    "dev:no-cheat": "pnpm --parallel --filter no-cheat-server --filter no-cheat-client dev",
    "dev:versus": "pnpm --parallel --filter versus-server --filter versus-client dev",
    "dev:list-builder": "pnpm --parallel --filter list-builder-server --filter list-builder-client dev",
    "dev:game-tracker": "pnpm --parallel --filter game-tracker-server --filter game-tracker-client dev",
    "dev:tournament": "pnpm --parallel --filter tournament-server --filter tournament-client dev",
    "build": "pnpm -r --filter '*-client' build",
    "test": "pnpm -r test",
    "lint": "eslint apps/*/server/src apps/*/client/src packages --ext .ts,.tsx",
    "lint:fix": "eslint apps/*/server/src apps/*/client/src packages --ext .ts,.tsx --fix",
    "deploy:no-cheat": "pnpm --filter no-cheat-server wrangler deploy && pnpm --filter no-cheat-client wrangler pages deploy dist",
    "deploy:versus": "pnpm --filter versus-server wrangler deploy && pnpm --filter versus-client wrangler pages deploy dist",
    "deploy:list-builder": "pnpm --filter list-builder-server wrangler deploy && pnpm --filter list-builder-client wrangler pages deploy dist",
    "deploy:game-tracker": "pnpm --filter game-tracker-server wrangler deploy && pnpm --filter game-tracker-client wrangler pages deploy dist",
    "deploy:tournament": "pnpm --filter tournament-server wrangler deploy && pnpm --filter tournament-client wrangler pages deploy dist",
    "deploy:new-meta": "pnpm --filter new-meta-server wrangler deploy && pnpm --filter new-meta-client wrangler pages deploy dist",
    "dev:admin": "pnpm --parallel --filter admin-server --filter admin-client dev",
    "sync:wahapedia": "pnpm --filter wahapedia-sync start",
    "sync:gw": "pnpm --filter gw-sync start"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "prettier": "^3.8.1",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "better-sqlite3"
    ]
  }
}
