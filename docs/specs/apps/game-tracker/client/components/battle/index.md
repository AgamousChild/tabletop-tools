# apps/game-tracker/client/src/components/battle/index.ts

> Barrel export for all battle sub-components and types.

## Prompt

Re-export all battle components and types from a single index file: `VpStepper`, `Scoreboard`, `StratagemPicker` (+ `StratagemEntry` type), `UnitPicker` (+ `DestroyedUnit` type), `SecondaryPicker` (+ `SecondaryMission` type), `PhotoCaptureScreen`, `CommandPhaseScreen`, `ActionPhaseScreen`, `TurnFlow`, `RoundSummary`, `RoundWizard`, and from `./types`: `TurnData`, `RoundStep`, `RoundState`, `createEmptyTurnData`.
