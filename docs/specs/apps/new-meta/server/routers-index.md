# apps/new-meta/server/src/routers/index.ts

> Root router — health + meta + player + source + admin.

## Prompt

Mount `metaRouter`, `playerRouter`, `sourceRouter`, `adminRouter` plus `health` public query. Import from local `../trpc.js`.
