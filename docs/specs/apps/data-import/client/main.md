# apps/data-import/client/src/main.tsx

> Vite entry point — same pattern as all apps.

## Prompt

Import `./index.css`, import `renderApp` from `@tabletop-tools/ui`, import `App` from `./App`. Call `renderApp(App)`. Three lines. See `docs/specs/apps/versus/client/main.md` for the canonical example.
