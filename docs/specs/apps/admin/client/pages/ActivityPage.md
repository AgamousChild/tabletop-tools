# apps/admin/client/src/pages/ActivityPage.tsx

> Per-app activity — total and 7-day counts with progress bars.

## Prompt

Show app activity from `trpc.stats.appActivity.useQuery()`. For each app: name, total count, 7-day recent count, progress bar (recent/total ratio).
