# apps/list-builder/server/src/server.ts

> Factory — same pattern as versus. No extended context.

## Prompt

Export `createServer(db: Db, secret: string)` that calls `createBaseServer({ router: appRouter, db, secret })`. Import router from `./routers`.
