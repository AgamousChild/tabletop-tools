# apps/brain/client/src/App.tsx + main.tsx

> Root component and Vite entry — renders BrainScreen directly.

## Prompt

`App` renders `<BrainScreen />`. No auth gate (brain is a public app). `main.tsx` imports CSS, calls `renderApp(App)`. Same pattern as data-import.
