# Session recap — 2026-07-27

## Live on prod
- **Brain worker** (`tabletop-tools.net/brain/`, v20260727-174257): shape registry (onboarding + list-review shapes), cubeRefs + connectedRefs on /ask response, `debug.cubeDispatched` flag, cube-based DP math, /ask cache, table renderer, honest web-only disclaimer, unit-name faction inference, subreddit-faction hint in eval harvester, defensive try/catch around shape dispatch.
- **Cube** rebuilt: 40k facts, 34 factions, 266 detachments with dp, 0 orphan flags (down from 2,610).
- **Turso** — migration 0014 applied (`meta_event_players.source_list_id`). 11 chapter aliases seeded in `dim_faction_alias`.

## Pending — next moves once you restart
1. **Run `bcp_scrape_lists`** for the Jun-Jul window. Creds are in .env now. From MCP after restart:
   ```
   mcp__ops__bcp_scrape_lists  since=2026-06-01  until=2026-07-31
   ```
   Or from CLI:
   ```
   cd apps/bcp-scraper/server
   set -a && source ../../../.env && set +a
   NODE_OPTIONS=--dns-result-order=ipv4first npx tsx scripts/scrape-lists.ts --since=2026-06-01 --until=2026-07-31
   ```
   Expected: ~9k army list texts fetched into `meta_event_players.list_text`. First bulk data for meta-driven `list-review` + `unit-viability` shape handlers.
2. **Weekly BCP cron** — add `BCP_EMAIL` + `BCP_PASSWORD` + `TURSO_*` to GH Actions repo secrets so the weekly workflow at `.github/workflows/bcp-scrape.yml` (from the earlier agent's worktree — merge if not merged yet) fires on schedule.
3. **`bcp_pipeline_full`** via MCP for the full events → lists chain going forward.

## Task list state (`TaskList` after restart)
- **#14** pending — 40K-themed loading spinner
- **#16** pending — Wahapedia shared-SM-datasheet-tagged-chapter-only fix (367 unit-abilities)
- **#19** pending — Run `bcp_scrape_lists` for Jun-Jul window (unblocked now that creds landed)
- **#15, #17, #18** completed

## Commits landed this session (`feat/brain-ai-gateway-model-swap`)
```
e3629d3 chore(meta): apply migration 0014 (source_list_id on meta_event_players)
ce07833 feat(bcp): consolidate BCP scrapers into apps/bcp-scraper + expose 3 ops MCP tools
cbd9fa5 fix(meta): seed dim_faction_alias for chapter names
304a89f fix(brain): defensive try/catch around shape registry dispatch in /ask
0bce19f feat(brain): question-shape plugin registry + list-review + onboarding shapes
ada8606 feat(brain): expose cubeRefs + connectedRefs (with titles) on /ask response
c4e73c9 feat(eval-ask): honest provenance grader + faction hint + Q&A report + classifier + backlog
fc46c8e feat(brain): infer faction from unit names in /ask when detector returns empty
3461f4d feat(eval-ask): pipeline scaffolding — arctic-shift harvest → /ask → grade → aggregate
750baf2 feat(brain): cube + /count endpoint + /ask query planner (deterministic counts)
+ several earlier fixes: detachment dedupe, honest provenance disclaimer, table renderer, /ask cache, etc.
```

## Data artefacts (gitignored)
- `scripts/eval-ask/.data/questions.jsonl` — 486 questions harvested from arctic-shift across 30 subs
- `scripts/eval-ask/.data/answers.jsonl` — /ask responses for all 486 (run 2, with faction hint prefix)
- `scripts/eval-ask/.data/grades.jsonl` — structural grades per question
- `scripts/eval-ask/.data/qa-report.md` — 1.35 MB markdown, one section per question. Full answers + refs + web sources + failure modes.

## Question-shape backlog (`scripts/eval-ask/SHAPES-BACKLOG.md`)
Captured from Micah's Q&A annotations:
- **transport-capacity** — "How many slots does Grimaldus take up in a Redeemer? What fits in the rest?" (all factions)
- **faction-vs-faction-strategy** — "What can my [Faction] do against [Enemy]?" (needs meta data)
- **unit-comparison** — "In my [Faction], which is better: [Unit A] or [Unit B]?" (needs meta data)

## Eval findings (run 2, 486 questions)
Empirical distribution of user question shapes (per `scripts/eval-ask/classify-questions.ts`):
| shape | % |
|---|---:|
| list-review | 15.6% |
| onboarding | 13.4% |
| unit-viability | 10.3% |
| lore-hobby | 8.2% |
| tournament-prep | 5.1% |
| list-help-abstract | 3.9% |
| rules-mechanic | 3.3% |
| disposition-play | 3.1% |
| social-meta | 2.5% |
| ability-source | 1.0% |
| ... (5 more <1%) | |
| UNCLASSIFIED | 52% |

**Provenance breakdown of answers (fixed grader):**
- brain-primary 8.2%, mixed 17.7%, web-only 57.2%, no-answer 16.9%.
- Cube dispatched on 93/486 questions (19%).
- 90% of answers still have zero Vectorize refs — retrieval is the biggest remaining gap.

## Open worktrees
```
agent-a8b4f08…  shape registry (already merged via cherry-pick 0bce19f)
agent-a3b58e17…  BCP scraper consolidation (merged into main ce07833)
agent-acebfdc8… BCP Jun-Jul backfill (script copied to main tree)
agent-aefcc40b… original BCP workflow agent (bcp-scrape.yml sits there — check if worth keeping)
+ several older worktrees from prior sessions (not part of this thread)
```

## Alt meta-data sources scouted
- **stats.hutber.com** — open GraphQL at `apistats.hutber.com/graphql`. 54k games. **FROZEN at 10e** ("maintenance mode" for 11e rebuild). Wait until they ship 11e.
- **Stat Check (Tableau)** — 10e only, requires Playwright to scrape Tableau session, high maintenance risk. Skip.
- **Tactical Tortoise YouTube** — **the only 11e-current source**. Weekly "Best Armies" videos. Extract via `yt-dlp` (already installed) → transcript → LLM structured extraction. Recommended second data source once BCP is flowing.

## Known bugs / rough edges NOT touched
- **/ask retrieval semantic-match gap** — the reason 90% of answers have zero refs. Vectorize retrieval doesn't hit for meta/strategic questions; unit-name inference helps for a subset but many questions don't name units. Real fix likely requires per-shape retrieval strategies OR upgrading the embedder / query rewriter.
- **Slow /ask response** — median 29s. Not investigated per your deprioritization.
- **Wahapedia shared-SM-datasheet-tagged-chapter-only** (task #16) — Gladius Lancer/Reaper/Valiant only under `black-templars` in the graph, not `space-marines`. Causes DA/BA/other-chapter faction inference to miss.

## Environment sanity
- IPv6 route to Turso is broken since 2026-07-06 outage — always run Node scripts with `NODE_OPTIONS=--dns-result-order=ipv4first`.
- `BCP_EMAIL` + `BCP_PASSWORD` now in `.env` — verified present as of this recap.
